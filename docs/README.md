# docs/

**Public** documentation for homespace. At v0.1.0 (TDD §11, WO-10) this docs site
is itself built as a homespace — dogfooding the kit.

Until then this holds public prose (getting started, archetype guides, theming).
Private planning and the design doc live in `../internal-docs/`, which is never
published.
