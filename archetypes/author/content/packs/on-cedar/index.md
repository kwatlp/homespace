# On Cedar

Long-form writing lives in a `post` pack's `index.md`. Markdown with GFM is
supported — **bold**, lists, tables, and [links](https://example.com).

- Add posts as `post` packs.
- Set `created` for ordering and the RSS feed.
