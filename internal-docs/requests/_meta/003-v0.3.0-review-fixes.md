---
id: 003
title: v0.3.0 review fixes — CI gates, app build slot, alt text, Builder polish
milestone: _meta
created: 2026-08-06
author: claude (chat review)
status: done
suggested_branch: fix/v0.3.0-review-fixes
---

## Status

**Done.** Items 1–9 landed in `3e3047e` and item 10 in `e8ba9e7`, both on
`fix/v0.3.0-review-fixes`; 1–9 reached `main` as PR #16 (`bd04efc`). CI green
throughout.

Item 9 was the last one open, because its AC could not be observed until
`docs.yml` first executed — which only happens on merge to `main`, and only
after **Settings → Pages → Source** is set to GitHub Actions once. It has now
run. The first two attempts died on a name collision this request never saw:
the workspace root and the launcher were both named `homespace`, so `npx
homespace build` in `docs/` could resolve the bin-less root ("could not
determine executable to run"). `fac7041` hot-fixed `docs.yml` from the web
editor to invoke the launcher's bin directly; run #3 published, and the
operator confirmed <https://kwatlp.github.io/homespace/builder/> loading on
2026-08-08. The wizard run behind that address is covered by the branch's
`file://` browser passes and the 18 wizard tests rather than a click-through on
Pages. Both READMEs carry the real address, replacing the placeholder. The
collision itself — not just the symptom `docs.yml` routes around — is fixed in
the closeout commit that renames the root package to `homespace-workspace`.

Two caveats from execution stand as a record (session handoff,
`003output.txt`): slug mirroring has a source-contract assertion rather than a
behavior test (no DOM harness in the §5.4 budget), and the Firefox `file://`
pass could not observe post-`await` DOM updates under headless screenshot
capture.

## Objective

Close the findings from the v0.3.0 re-review (chat, 2026-08-06) so the
Builder honors every ruled acceptance criterion and the repo gains the CI it
has never had. Review basis: independent verification at tag `v0.3.0` —
172/172 tests and typecheck green, bundle artifacts byte-identical to a
fresh rebuild, all six ruled WO-12 items confirmed in code.

## TDD references

- §5.3 a11y baseline — alt-text collection (item 3)
- §6.4 sandbox posture — unchanged; preview stays `allow-scripts` only
- §15 (Builder section) — items 2–8 refine WO-22 behavior
- CLAUDE.md gates — item 1 makes them run on every push, not just locally

## Context

The gates (`tsc --build`, vitest, bundle freshness) exist but only run when
someone remembers. `docs/static/builder/` is committed build output with no
freshness guard. The wizard's single `build` slot only feeds **game** packs —
apps silently get the placeholder, against the ruled "plug in assets for
each field." Plugged gallery images ship `alt="Picture N"` because nothing
collects descriptions. And the page loads via
`<script type="module">`, which browsers refuse from `file://` — so the
"download it and double-click" story quietly breaks.

## Scope

- **In scope:** `.github/workflows/`, `packages/builder` (wizard, ui, page,
  bundle script), the renderer head/link emission only as far as item 7
  requires, docs touched by items 3 and 8.
- **Out of scope / do NOT touch:** folder-picker upload for subdirectory
  builds (future WO), WO-13+ roadmap items, contracts §3/§4 beyond what
  item 7 needs, the serve daemon.

## Requirements (acceptance criteria)

### P1 — the guard rail

- [x] **1. CI workflow.** `.github/workflows/ci.yml` on push + PR: Node 20,
      `npm ci`, `tsc --build`, `vitest run`, then rebundle
      (`node packages/builder/scripts/bundle.mjs`) and
      `git diff --exit-code docs/static/builder` so committed artifacts can
      never drift from their sources. AC: workflow is green on this PR;
      editing page source without rebundling makes the diff step fail
      (demonstrated locally in the PR description).

### P2 — ruled-AC gaps

- [x] **2. Apps get their own build slot.** Separate plugged-build state and
      file inputs per kind (game / app), each flat multi-select with the
      existing `index.html` check. Plugging into either lands in that pack;
      plugging into both works at once; wizard tests cover each path.
- [x] **3. Alt text is collected.** After gallery images are chosen, the
      wizard shows one short description field per image; entered text lands
      in the pack's `media.alt` map, blank keeps today's fallback, and
      `START-HERE.md` teaches editing alts later. AC: a11y baseline (§5.3)
      pass on a wizard-produced site with described images.
- [x] **4. The Builder runs from a double-click.** Bundle the page as a
      classic script (esbuild `iife`) or equivalent so
      `docs/static/builder/index.html` works from `file://` — module scripts
      are CORS-blocked there today. AC: opening the file from disk runs the
      full wizard in Chromium and Firefox; the served path is unchanged; the
      freshness gate covers the new artifact shape.
- [x] **9. Docs auto-deploy (ruled).** On every merge to `main`, CI builds
      the docs homespace (`npx homespace build` in `docs/`) and publishes
      its `dist/` to GitHub Pages — the Builder gets a permanent public
      address that can never drift from the repo. AC: the Builder loads and
      completes a full wizard run at the Pages URL; every internal link
      survives the project-subpath (`/homespace/`) — verify the renderer's
      relative URLs hold there and fix any that don't; README and the setup
      instructions gain the real address, replacing the placeholder.

### P3 — polish

- [x] **5. Slug field mirrors state.** The input shows the slugified value
      the state actually holds, so the download filename never surprises.
- [x] **6. Un-pick an icon or banner.** A "use the placeholder instead"
      control clears a chosen asset; cancelling a re-pick keeps the prior
      choice.
- [x] **7. Icons keep their format.** An uploaded icon is written with its
      real extension and the page links it with the matching `type`, instead
      of any bytes being named `favicon.ico`. Mechanism open; outcome fixed.
- [x] **8. Flat-build limit is visible.** A hint under each build input says
      flat files only — no subfolders; foldered exports (Unity-style) go in
      via the master copy — and the docs say the same.

### P4 — repo hygiene

- [x] **10. Request-pipeline cleanup (ruled).** `code-requests/` never took:
      both live requests (001, 002) landed flat in `internal-docs/`, leaving
      the root folder an empty shell (its README plus `_meta/.gitkeep`). Fold
      reality in rather than fighting it: create `internal-docs/requests/`
      keeping the documented `_meta/`–`WO-n/` layout and global numbering, move
      the existing requests into their proper subfolders, and delete root
      `code-requests/`. Then dedupe the three READMEs to one home per concern:
      the request process (layout, IDs, lifecycle, how to write one) merges
      into `internal-docs/requests/README.md`; `internal-docs/README.md` stays
      the private-planning index with corrected pointers; `docs/README.md` gets
      refreshed to v0.3 reality — it still says the dogfooded docs site is
      coming "at v0.1.0", while the site is here, carries the Builder, and item
      9 publishes it (add the Pages URL once live). Update every reference:
      root `README.md`, `CLAUDE.md`, `CONTRIBUTING.md`, the template, and the
      request files' own path mentions. AC: no `code-requests/` at root, no
      duplicated process prose anywhere, every link resolves.

## Verification

- CI green on the PR itself (item 1 proves the others).
- New tests: app slot, alt plumbing into `media.alt`, slug mirroring.
- Manual: `file://` run in two browsers (item 4); phone-width pass over the
  new inputs per the mobile-first rule.

## Git / publish

- **Commit message:** `fix: v0.3.0 review fixes (CI, app builds, alt text, file:// builder)`
- **Branch / PR:** `fix/v0.3.0-review-fixes` → PR to `main`
- **Push?** leave to the human.

## References

- `internal-docs/requests/_meta/001-v0.2.0-review-remediations.md` — roadmap context
- `internal-docs/requests/WO-21/002-the-builder.md` — the ruled ACs items 2–3 restore
- v0.3.0 re-review conversation (Claude chat, 2026-08-06)

## Notes for the human / Claude Code

- This request is expected to **absorb findings from the Samsung S26
  Ultra + PC pipeline test** before execution — append them as items 10+
  rather than opening a fourth request.
- Item 4 changes the committed artifact shape; land it and the item-1
  freshness gate in the same PR so the gate never lies.
- Folder-picker builds stay future work on purpose; item 8 just stops the
  limit from being a surprise.
